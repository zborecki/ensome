import { DefaultTheme } from 'styled-components';
import colors from './colors';

export default {
  header: {
    navigation: {
      color: {
        default: colors.grey,
        active: colors.black
      }
    }
  }
} as DefaultTheme;
